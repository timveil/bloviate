# Bloviate Benchmarks

The benchmark methodology and recorded results — JMH CPU micro-benchmarks and end-to-end fill
throughput against real databases, with the commands to reproduce them — live on the documentation
site:

**→ [bloviate.io/guides/benchmarks](https://bloviate.io/guides/benchmarks/)**

The benchmark code is in [`bloviate-benchmarks/`](bloviate-benchmarks/), and the canonical Markdown
source is [`docs/BENCHMARKS.md`](docs/BENCHMARKS.md); the site renders it.
